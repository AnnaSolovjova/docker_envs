This container is there to act as a 'sandbox' for node 
When container is running the app.js (and the whole app.js folder) can be modified and the changes picked up in the container.

Run docker-compose up